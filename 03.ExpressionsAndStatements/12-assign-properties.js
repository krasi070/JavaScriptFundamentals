function assignProperties([firstKey, firstValue, secondKey, secondValue, thirdKey, thirdValue]) {
    let obj = {};
    obj[firstKey] = firstValue;
    obj[secondKey] = secondValue;
    obj[thirdKey] = thirdValue;

    return obj;
}